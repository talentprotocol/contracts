export const source = ({ spkgPath, spkgModule }) => `
      package:
        moduleName: ${spkgModule || 'graph_out'}
        file: ${spkgPath || 'substreams.spkg'}`;
export const mapping = () => `
      apiVersion: 0.0.5
      kind: substreams/graph-entities`;
