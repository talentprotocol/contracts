import Protocol from '../protocols/index.js';
export declare const validateManifest: (value: any, type: any, schema: any, protocol: Protocol, { resolveFile }: {
    resolveFile: (path: string) => string;
}) => any;
